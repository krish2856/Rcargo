{
  "name": "rcargo-root",
  "version": "1.0.0",
  "description": "Rcargo project root",
  "scripts": {
    "install:all": "npm install --prefix backend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}
